services:
    redis:
        image: redis:7
        ports:
            - "6379:6379"
    app:
        build: .
        ports:
            - "3000:3000"
        environment:
            NODE_ENV: production
            PORT: 3000
            REDIS_HOST: redis
            REDIS_PORT: 6379
        depends_on:
            - redis
